public class Solution {
    public int LengthOfLastWord(string s) {
        int wordLength = 0;
        
        for(int i = s.Length - 1; i >= 0; i--){
            if(s[i] != ' ') wordLength++;
            else if(wordLength > 0) break;
        }

        return wordLength;
    }
}