public class Solution {
    public IList<int> GrayCode(int n) {
        int amount = 1 << n;
        int[] result = new int[amount];
        
        for(int i = 0; i < amount; i++){
            result[i] = (i ^ (i >> 1));
        }

        return result;
    }
}